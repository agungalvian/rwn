version: '3.8'

services:
  app:
    build: .
    container_name: rawinda_finance_app
    ports:
      - "3000:3000"
    volumes:
      - ./public/uploads:/app/public/uploads
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DB_HOST=db
      - DB_USER=postgres
      - DB_PASSWORD=rawinda_secret
      - DB_NAME=rawinda_finance
    depends_on:
      - db
    restart: always

  db:
    image: postgres:15-alpine
    container_name: rawinda_finance_db
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=rawinda_secret
      - POSTGRES_DB=rawinda_finance
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: always

volumes:
  postgres_data:
